# Zig build artifacts
.zig-cache/
zig-out/
# Stray local WASM droppings at repo root (canonical output: zig build wasm → zig-out/web/)
/root.wasm
/*.wasm

# Python virtual environment
.venv/
__pycache__/

# IDE configuration
.opencode/
.vscode/
.idea/
.playwright-mcp/

# Model weights (large files)
/models/
*.gguf
*.safetensors

# Compiled artifacts
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# Temporary kernel compilation artifacts (use `zig build ptx`/`zig build amdgcn` instead)
test_*.s
test_*.ll
test_*.spv

# Research kernel artifacts
research/kernels/**/*.ptx
research/kernels/**/*.spv
research/kernels/**/*.cubin
research/kernels/**/*.fatbin
research/kernels/golden/*.bin

# OS-specific
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Test harness artifacts
tests/.venv/
tests/traces/
tests/results/
tests/golden_outputs/

# Logs
*.log

# Environment and secrets
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx

# Claude code artifacts
.claude/

# Cross-compiled output directory
/zig-out-linux/

# Debug tensor dumps (vision / model debugging)
/*.bin
/vision_ref_dumps/
research/kernels/vision_ref_dumps/

# Test images (generated locally, not needed in repo)
/test_*.png

# Stray operator manifests (not part of this project)
pelorus-*.yaml

# Optional offline diagram renders (Mermaid in tutorials is the source of truth)
# Uncomment to stop tracking local re-renders: docs/diagrams/

docs/changelogs/

# Local scratch / generated files
graphify-out/
.ralph/
.worktrees/
.superpowers/
docs/superpowers/
glossary-batch*.md
outputs/
progress.md
