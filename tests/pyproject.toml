[project]
name = "agave-e2e-harness"
version = "0.1.0"
description = "End-to-end correctness harness (not shipped in the binary)"
requires-python = ">=3.11"
dependencies = [
    "rich==15.0.0",
]

[tool.uv]
# Script-only harness; nothing here is an importable package.
package = false
